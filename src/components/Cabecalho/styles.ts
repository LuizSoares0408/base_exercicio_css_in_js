import styled from 'styled-components'
import { Root } from '../../styles'

export const Cabecalho = styled.header`
  background-color: ${Root.corSecundaria};
  color: ${Root.corPrincipal};
  text-align: center;
  padding: 24px 0;
`
